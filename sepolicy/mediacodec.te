# For enumerating GPU devices (access to the chr_file is granted in the
# base policy).
allow mediacodec gpu_device:dir r_dir_perms;
# To access GPU-related files in sysfs.
allow mediacodec gpu_device:file r_file_perms;
